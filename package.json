{
  "name": "@crow-b3/local-dev",
  "version": "0.0.1",
  "description": "Clone and sync all CROW-B3 repositories",
  "type": "module",
  "scripts": {
    "clone": "bun run src/clone.ts",
    "sync": "bun run src/sync.ts",
    "update": "bun run src/update.ts",
    "pr": "bun run src/pr.ts",
    "clone:all": "bun run src/clone.ts --all",
    "sync:force": "bun run src/sync.ts --force",
    "update:all": "bun run src/update.ts --all",
    "pr:update": "bun run src/pr.ts --force"
  },
  "devDependencies": {
    "@types/bun": "1.3.5",
    "@types/yargs": "17.0.35"
  },
  "keywords": [
    "crow-b3",
    "local-dev"
  ],
  "author": "CROW-B3",
  "dependencies": {
    "yargs": "18.0.0"
  }
}
